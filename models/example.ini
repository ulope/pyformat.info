[model]
name = Example
label = {{ this.title|default(this._id, True) }}

[fields.title]
label = Title
type = string
size =  large

[fields.sort]
label = Sort order
type = sort_key

[fields.examples]
label = Examples
type = flow
flow_blocks = example_block
